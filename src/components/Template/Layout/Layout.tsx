import Navbar from 'components/Organisms/Navbar';
import { StyleWrapper } from './style';

const Layout = () => {
    return (
        <StyleWrapper>
            <Navbar />
        </StyleWrapper>
    );
};

export default Layout;
